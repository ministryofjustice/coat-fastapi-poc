[project]
name = "coat-fastapi-poc"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.78",
    "fastapi[standard]>=0.141.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
